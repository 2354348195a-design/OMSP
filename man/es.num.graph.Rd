% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/es_num_group.R
\name{es.num.graph}
\alias{es.num.graph}
\title{Estimate Optimal Number of Clusters from Graph}
\usage{
es.num.graph(W, NUMC = 2:8)
}
\arguments{
\item{W}{A symmetric affinity matrix (graph weight matrix).}

\item{NUMC}{An integer vector specifying candidate numbers of clusters.
Default is \code{2:8}. The method assumes more than one cluster.}
}
\value{
A list with the following elements:
\describe{
\item{first}{The cluster number with the best (minimum) rotation cost.}
\item{second}{The cluster number with the second-best rotation cost.}
\item{value}{A numeric vector of rotation cost values corresponding to \code{NUMC}.}
}
}
\description{
This function estimates the optimal number of clusters based on a given
affinity graph by evaluating rotation cost using spectral clustering
eigenvectors. It computes the normalized Laplacian, obtains eigenvectors,
applies discretisation, and evaluates clustering quality for candidate
cluster numbers.
}
\details{
The function:
\itemize{
\item symmetrizes the affinity matrix
\item constructs the normalized Laplacian
\item computes the eigenvalues and eigenvectors
\item applies a discretisation method to obtain approximate cluster
membership
\item evaluates a rotation cost–based quality index
}

The two numbers of clusters with the smallest rotation cost are returned.
}
\examples{
\dontrun{
W <- matrix(runif(100), 10, 10)
W <- (W + t(W)) / 2
result <- es.num.graph(W, 2:6)
print(result)
}

}

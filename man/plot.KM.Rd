% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_KM.R
\name{plot.KM}
\alias{plot.KM}
\title{Plot Kaplan–Meier Survival Curves}
\usage{
\method{plot}{KM}(
  clinical,
  labels,
  limit = NULL,
  annot = NULL,
  color = NULL,
  xlab = "Follow up",
  ylab = "Survival Probability",
  title = NULL,
  legend.pos = "top",
  palette = "jama_classic",
  risk.table = TRUE,
  risk.table.ratio = 0.4,
  anno.pos = "bottom",
  anno.x.shift = 0.5
)
}
\arguments{
\item{clinical}{A survival object created by \code{Surv(time, event)}
or a two-column matrix where the first column is survival time
and the second column is event indicator (1 = event, 0 = censored).}

\item{labels}{A vector of group labels corresponding to each sample.}

\item{limit}{Optional. A numeric cutoff for truncating survival time.
Values above the limit are set to the limit and marked as censored.}

\item{annot}{Optional. Additional annotation text to display on the plot.}

\item{color}{Optional. A vector of custom colors. Can be named.
If NULL, colors are automatically selected from \code{palette}.}

\item{xlab}{Label for the x-axis.}

\item{ylab}{Label for the y-axis.}

\item{title}{Title of the plot.}

\item{legend.pos}{Position of the legend (e.g. "top", "bottom").}

\item{palette}{Color palette name. Options include "jama_classic",
"nature", "jco", "lancet", etc.}

\item{risk.table}{Logical. Whether to display the risk table
below the survival curve. Default is TRUE.}

\item{risk.table.ratio}{The ratio between the heights of the KM plot
and the risk table.}

\item{anno.pos}{Position of annotations ("bottom" or "right").}

\item{anno.x.shift}{Horizontal shift factor when annotation is
placed on the right.}
}
\value{
A ggplot object or a combined plot object created by \code{cowplot}.
}
\description{
Generates Kaplan–Meier survival curves for multiple groups with
options for p-value display, hazard ratio calculation, risk table,
annotations, and customizable color palettes.
}
\examples{
\dontrun{
clinical <- Surv(c(5, 10, 8), c(1, 0, 1))
labels <- c("A", "B", "A")
plot.KM(clinical, labels)
}
}
